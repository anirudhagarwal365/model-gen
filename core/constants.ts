export enum CLASS_TYPE {
    CLASS = 'CLASS',
    ENUM = 'ENUM'
}

export enum FIELD_TYPES {
    STRING = 'STRING',
    OBJECT = 'OBJECT',
    BOOLEAN = 'BOOLEAN',
    INTEGER = 'INTEGER',
    FLOAT = 'FLOAT',
    LONG = 'LONG',
    LIST = 'LIST',
    MAP = 'MAP',
}

export enum SUPPORTED_LANGUAGES {
    TYPESCRIPT = 'typescript',
    PYTHON = 'python',
}